# Veritabanı (Railway otomatik sağlar)
DATABASE_URL=postgresql://user:password@localhost:5432/ticaretsim

# JWT imzalama anahtarı (güçlü, rastgele bir string kullan)
JWT_SECRET=supersecretkey_change_this_in_production

# Sunucu portu (Railway otomatik ayarlar)
PORT=3000

# Ortam
NODE_ENV=development
